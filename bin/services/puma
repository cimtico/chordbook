#!/usr/bin/env bash
set -Eeuo pipefail

SCRIPT_NAME="${0##*/}"
SCRIPT_DIR="${0%/*}"

cd "${SCRIPT_DIR}" >/dev/null 2>&1 || exit 1

# Switch context to the root of the app
cd ../.. >/dev/null 2>&1 || exit 1

echo "Starting worker"
exec bundle exec puma -p 3100 -b tcp://0.0.0.0